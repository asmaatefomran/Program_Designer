# Base URL of the ProgramDesigner.Api backend.
# Local dev (dotnet run): http://localhost:5000
# docker-compose: leave as http://localhost:5000 (the backend container publishes to that host port)
VITE_API_BASE_URL=http://localhost:5000
